{% extends '::base.html.twig' %}

{% form_theme form 'ClientBundle::fields.html.twig' %}

{% block body %}
    <ol class="breadcrumb">
        <li><a href="{{ path('client_project_list') }}">Projects</a></li>
        <li class="active">Edit project : {{ project.name }}</li>
    </ol>

    <h1>Edit project : {{ project.name }}</h1>

    {{ form(form) }}
{% endblock body %}
